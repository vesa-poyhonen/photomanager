<nav class="navbar navbar-expand-lg navbar-light bg-light mb-5">
  <a class="navbar-brand" href="<%= root_path %>">Application</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if is_logged_in? && current_user %>
        <li class="nav-item<%= ' active' if controller.controller_name == 'pictures' and controller.action_name == 'index' %>">
          <a class="nav-link" href="<%= pictures_path %>">My pictures</a>
        </li>
        <li class="nav-item<%= ' active' if controller.controller_name == 'pictures' and controller.action_name == 'new' %>">
          <a class="nav-link" href="<%= new_picture_path %>">Upload</a>
        </li>
        <% if session[:access_token].nil? %>
          <li class="nav-item">
            <a class="nav-link" href="<%= oauth_request_path %>">Connect to MyTweetApp</a>
          </li>
        <% else %>
          <li class="nav-item">
            <span class="nav-link text-success">Connected to MyTweetApp</span>
          </li>
        <% end %>
        <li class="nav-item">
          <a class="nav-link" href="<%= logout_path %>">Logout</a>
        </li>
      <% else %>
        <li class="nav-item<%= ' active' if controller.controller_name == 'sessions' %>">
          <a class="nav-link" href="<%= login_path %>">Login</a>
        </li>
      <% end %>
    </ul>
  </div>
</nav>